# Zigomponents

Zigomponents are HTML components written in Zig. The inspiration is derived from [gomponents](https://github.com/maragudk/gomponents). This project currently depends on zig master.

## Building this project
- Download and install zvm to manage different versions of zig.
- Run `zvm install master` to install the latest version of zig.
- Run `zvm use master` to use the latest version of zig.
- Run `zig build` to build the project.

## Features
- Features here

## Usage
- Usage here
